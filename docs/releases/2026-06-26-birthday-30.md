# Birthday Aloha Joel-y

Release id: `birthday-30`

Adds a birthday-week Joel-y variant with a bright Hawaiian shirt, oversized readable flower shapes, and a cone birthday party hat carried through the animated spritesheet.

Visual acceptance criteria verified in the generated contact sheet:

- Hawaiian shirt is visible and distinct from the previous golf outfit.
- Oversized flower shapes are visible on the shirt at pet size.
- Cone birthday party hat is visible on Joel-y's head across populated animation states.
- Joel-y identity remains intact: blond hair, sunglasses, face shape, and sticker-like style.

Package files:

- `releases/birthday-30/pet.json`
- `releases/birthday-30/spritesheet.webp`
- `releases/birthday-30/contact-sheet.png`
- `releases/birthday-30/release.json`

Install:

```bash
./scripts/install-or-update.sh --release birthday-30
```

Install the latest default:

```bash
./scripts/install-or-update.sh --latest
```
