# Joel-y Update

Joel-y has a new update: `<short summary>`.

What changed:

- `<change 1>`
- `<change 2>`
- `<change 3>`

Update locally:

```bash
cd joel-y
git pull
./scripts/install-or-update.sh
```

If Joel-y does not refresh immediately, restart Codex.

Repo: https://github.com/homelight/joel-y
